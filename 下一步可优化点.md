# 下一步可优化点：流式数据传输粒度与工具调用卡顿

## 背景

上游 [ds2api](https://github.com/CJackHwang/ds2api) 项目针对 DeepSeek SSE 流式输出的数据粒度做了系统性优化，显著改善了 write/edit 等工具调用时的卡顿问题。以下分析基于上游代码 `D:\git_repos\origin_ds2api\ds2api` 与本地项目 `D:\git_repos\ds2api` 的对比。

## 当前本地项目状态

`internal/sse/stream.go` 目前是原始的**逐行立即发送**版本（约54行）：
- 每收到一行 SSE 数据，立即解析并发送给下游
- 无累积缓冲、无粒度控制、无增量工具调用支持

## 上游优化演进（三个阶段）

### 阶段一：SSE 微批处理 — 减少碎片化发送

**提交**: `89225c7` — `fix(sse): batch tiny stream chunks before emitting` (2026-04-29)

**问题**: DeepSeek 上游返回的 SSE 数据行非常细碎（可能每行只有几个字符），逐行立即发送导致大量小数据包，增加网络往返和客户端渲染开销。

**方案**: 在 `StartParsedLinePump` 中引入累积缓冲：

| 参数 | 值 |
|------|-----|
| `MinFlushChars` | 160 |
| `MaxFlushWait` | 80ms (ticker) |

- 普通文本行先累积到 `pending` 缓冲区，达到阈值或超时才 flush
- Stop/Error/ContentFilter 等控制类消息不参与累积，立即透传

---

### 阶段二：增量工具调用流式输出 — 解决 write/edit 卡顿的核心

**提交**: `7beeea5` — `feat: Implement streaming incremental tool call deltas with a new tool sieve and standalone parser` (2026-02-18)

**问题**: 工具调用必须等到完整的 JSON 对象接收完毕才能解析和发送，导致 write（文件内容可能几 KB）等大型工具调用时客户端长时间无响应。

**核心变更** (`internal/adapter/openai/tool_sieve.go`):

1. **新增 `toolCallDelta` 结构体**，支持增量发送：
   ```go
   type toolCallDelta struct {
       Index     int
       Name      string    // 工具名称（先发送）
       Arguments string    // 参数增量片段（逐字符跟进）
   }
   ```

2. **`buildIncrementalToolDeltas`** — 在 JSON 仍在接收过程中实时解析：
   - 检测到 `tool_calls` 键后开始跟踪
   - 一旦识别出工具名称，立即发送 `ToolCallDeltas`（含 `Name`）
   - 参数部分（`arguments`）逐字符增量发送
   - 设置 `toolSieveCaptureLimit = 8KB`，超过则降级为纯文本输出

3. **新增 `ParseStandaloneToolCalls`** (`internal/util/toolcalls.go`) — 独立解析器，不依赖外层消息结构

**效果**: 对于 write 工具，客户端不再需要等待完整 JSON，而是边接收边展示，TTFT 从"等完整个工具调用"变为"几乎立即看到工具名"。

---

### 阶段三：PR #406 — TTFT 优化与缓冲延迟大幅降低

**提交**: `d407ccb` — `perf(streaming): optimize TTFT and reduce buffering latency` (2026-05-02)

**参数变更**:

| 参数 | 阶段一 | 阶段三 |
|------|--------|--------|
| `MinChars` | 160 | **16** |
| `MaxWait` | 80ms | **10ms** |
| `FlushOnNewline` | 无 | **立即发送** |
| 首块发送 | 需要等累积 | **立即发送** |

**架构变更** (`internal/sse/stream.go` 重写为 362 行):

```go
var productionAccumulate = AccumulateConfig{
    Enabled:        true,
    MinChars:       16,          // 16字符就触发发送
    MaxWait:        10 * time.Millisecond,  // 10ms超时
    FlushOnFinish:  true,
    FlushOnNewline: true,        // 遇到换行立即flush
}
```

**关键改进点**:

1. **首块立即发送（first-flush-immediate）**: `!anyFlushed` 条件确保第一个 chunk 不等待累积阈值，直接优化 TTFT（Time To First Token）

2. **三条独立 buffer 管线**:
   - `textBuffer` — 普通文本
   - `thinkingBuffer` — 思考内容（单独处理，不阻塞文本）
   - `toolDetectionThinkingBuffer` — 工具检测期间的思考内容

3. **`shouldFlushImmediate`** — 遇到 `\n` 或 `\r` 立即 flush

4. **独立的 scanner goroutine**: 读取与事件循环解耦，读取不再阻塞事件处理

5. **`TrimContinuationOverlapFromBuilder`** (`internal/sse/dedupe.go`): 基于 `strings.Builder` 的去重方法，避免每次去重都做 `String()` 转换，减少内存分配

**JS 端同步优化** (`internal/js/chat-stream/stream_emitter.js`):

```javascript
// 旧
const MIN_DELTA_FLUSH_CHARS = 160;
const MAX_DELTA_FLUSH_WAIT_MS = 80;
// 新
const MIN_DELTA_FLUSH_CHARS = 16;
const MAX_DELTA_FLUSH_WAIT_MS = 20;
```

**Claude stream runtime 集成** (`internal/httpapi/claude/stream_runtime_core.go`):
- 集成 `toolstream.ProcessChunk` 进行增量工具调用检测
- 用 `toolDetectionThinking` 累积 thinking 内容后去重
- `finalize` 阶段使用 `toolstream.Flush` 处理剩余缓冲区

---

### 补充：Scanner → Reader 重构

**提交**: `a901250` — `refactor: replace bufio.Scanner with bufio.Reader for SSE stream parsing` (2026-05-02)

- 将 `bufio.Scanner` 替换为 `bufio.Reader` + `ReadBytes('\n')`
- 解决 Scanner 的 2MiB 行长度限制
- 跟踪已发送文本防止重复输出

---

## 本地 vs 上游 差距总结

| 维度 | 本地版本 | 上游最新 |
|------|---------|---------|
| `stream.go` 行数 | ~54 行 | 362 行 |
| 发送粒度 | 每行立即发送 | 累积 16 字符 / 10ms / 换行即发 |
| TTFT | 依赖于行大小 | 首块立即发送 |
| 工具调用输出 | 等待完整 JSON | 增量 delta 流式发送 |
| 缓冲架构 | 无 | 3 条独立 buffer + scanner goroutine |
| 去重机制 | 无 | Builder 级别去重 |
| Scanner 行限制 | Scanner 2MiB 限制 | Reader 无限制 |

## 建议实施优先级

### 高优先级
1. **合入 accumulation buffer 架构** — `stream.go` 的 `startParsedLinePumpWithConfig` 完整实现
2. **合入增量工具调用 delta** — `tool_sieve.go` 的 `buildIncrementalToolDeltas` 及相关结构体

### 中优先级
3. **`dedupe.go` 的 `TrimContinuationOverlapFromBuilder`**
4. **JS 端 `stream_emitter.js` 的 DeltaCoalescer 阈值调整**

### 低优先级
5. **Scanner → Reader 重构** (本地已经使用 `bufio.Reader`)

## 涉及的上游文件清单

| 文件 | 用途 |
|------|------|
| `internal/sse/stream.go` | 核心累积缓冲与流控 |
| `internal/sse/dedupe.go` | 去重（含 Builder 版本） |
| `internal/adapter/openai/tool_sieve.go` | 工具调用增量解析 |
| `internal/util/toolcalls.go` | `ParseStandaloneToolCalls` |
| `internal/httpapi/claude/stream_runtime_core.go` | Claude 适配层集成 toolstream |
| `internal/httpapi/claude/stream_runtime_finalize.go` | finalize 阶段 toolstream flush |
| `internal/httpapi/openai/chat/chat_stream_runtime.go` | OpenAI 适配层集成 |
| `internal/js/chat-stream/stream_emitter.js` | 前端 DeltaCoalescer 阈值 |

## 上游关键提交列表

```
89225c7 fix(sse): batch tiny stream chunks before emitting
7beeea5 feat: Implement streaming incremental tool call deltas with a new tool sieve and standalone parser.
d407ccb perf(streaming): optimize TTFT and reduce buffering latency
a901250 refactor: replace bufio.Scanner with bufio.Reader for SSE stream parsing
```