# Claude Code 工具调用修复说明

日期：2026-05-06

## 背景

将 ds2api 接入 Claude Code 后，DeepSeek 经常输出 DSML 工具调用文本，但 Claude Code 没有真正执行工具，典型表现包括：

- `Write` 调用在 Claude Code UI 中显示为参数缺失或空 `file_path`。
- DeepSeek 原始响应里实际包含 `file_path`，但文件没有写入。
- 搜索工具、文件写入工具有时被当成普通文本输出，而不是 Claude `tool_use`。

## 核心问题

问题不在 Claude Code 的 Write 工具本身，也不总是模型没有生成参数。核心问题有三类：

1. **DSML 外层闭合标签经常畸形**

   DeepSeek 会输出类似：

   ```xml
   </|DSML>
   ```

   或：

   ```xml
   </|DSML|
   ```

   而不是规范的：

   ```xml
   </|DSML|tool_calls>
   ```

   旧逻辑只要发现整体 DSML wrapper 畸形，就完全跳过最终工具解析，导致内部完整的 `<|DSML|invoke name="Write">...</|DSML|invoke>` 没有被转换为 Claude `tool_use`。

2. **dev capture 配置没有稳定生效**

   `devcapture.Configure()` 设置了 config 中的 `dev_capture` 后，后续 `devcapture.Global()` 会再次按环境变量/默认值初始化，把配置覆盖回默认值。

   结果是：

   - `config.json` 中设置了 `limit: 20`，但 `/admin/dev/captures` 仍显示 `limit: 5`。
   - 无法稳定看到 Claude Code 发给 ds2api 的原始 `messages/tools`。
   - 难以判断 `file_path` 是模型没生成、ds2api 没解析，还是 Claude Code 没执行。

3. **缺少 Claude 入站和出站工具调用诊断**

   之前 capture 主要覆盖 DeepSeek completion，缺少：

   - Claude Code 发来的原始 `messages`、`tools`、`tool_choice`。
   - ds2api 最终返回给 Claude Code 的 `tool_use`。

   因此排查时只能看 DeepSeek 原始输出，无法直接判断工具调用是否已经被 ds2api 正确转换。

## 核心修改

1. **允许从畸形 DSML wrapper 中提取完整工具调用**

   修改 `internal/adapter/claude/stream_runtime_finalize.go`：

   - 旧逻辑：`HasMalformedToolCallFragment(finalText)` 为 true 时直接跳过解析。
   - 新逻辑：最终阶段先尝试 `ParseToolCallsDetailed(finalText, toolNames)`，只要内部有完整闭合的 `invoke` 且 required 参数满足 schema，就转换为 Claude `tool_use`。
   - dangling CDATA 等不完整参数仍不会执行。

2. **修复 dev capture 全局配置生命周期**

   修改 `internal/devcapture/store.go`：

   - 移除 `sync.Once` 反复重置导致的配置覆盖问题。
   - `Configure(settings)` 后，后续 `Global()` 返回同一个已配置 store。
   - `config.json` 中的 `dev_capture.enabled/limit/max_body_bytes` 能稳定生效。

3. **增加 config 驱动的 dev capture**

   修改配置相关文件：

   - `internal/config/config.go`
   - `internal/config/codec.go`
   - `internal/config/store_accessors.go`
   - `internal/server/router.go`
   - `internal/deepseek/client_core.go`

   新增 `dev_capture` 配置项，并在服务启动时记录实际加载的：

   - pid
   - cwd
   - config_path
   - keys/accounts 数量
   - dev_capture 是否启用
   - capture limit
   - max body bytes

4. **记录 Claude Code 入站请求和工具转换结果**

   修改 `internal/adapter/claude/handler_messages.go` 和 stream runtime：

   - 新增 `claude_inbound` capture，记录 Claude Code 发来的 `messages/tools/tool_choice/max_tokens`。
   - 新增 `claude_tool_use` capture，记录 ds2api 最终转换出的工具名和 input。

5. **补充 required 参数缺失防护**

   修改：

   - `internal/util/toolcalls_schema_normalize.go`
   - `internal/format/claude/render.go`
   - `internal/adapter/claude/stream_runtime_core.go`

   当工具调用缺少 required 字段（例如 `Write.file_path` 为空）时：

   - 不执行工具。
   - 不把原始 DSML 泄漏给 Claude Code 当普通文本。
   - 返回明确提示：工具调用缺少必填参数。

## 回归测试

新增/调整测试覆盖：

- DeepSeek 输出 `</|DSML>` 或 `</|DSML|` 这类畸形外层 wrapper，但内部 `Write` invoke 完整时，仍能生成 Claude `tool_use`。
- `file_path` 和 `content` required 参数为空时，不执行 `Write`。
- dangling CDATA 未闭合时，不执行工具。
- `devcapture.Configure()` 后 `Global()` 不再覆盖配置。

验证命令：

```powershell
go test ./internal/util ./internal/adapter/claude -count=1
go test ./... -count=1
```

## 相关提交

- `93cd8b6 feat: add config-backed Claude capture diagnostics`
- `fea5556 fix: execute complete tool calls with malformed DSML wrapper`
- `ac91ace fix: persist dev capture config and log Claude tool uses`

---

# OpenAI 链路 DSML 工具调用修复说明

日期：2026-05-07

## 背景

在 `/v1/chat/completions` 主链路下，DeepSeek 已经多次输出了肉眼可见的 DSML 工具调用，但 ds2api 没有真正拦截执行，典型表现包括：

- `Write` 明明已经输出 `<|DSML|invoke name="Write">`，但没有真正写文件。
- `Bash` 在前端显示里看起来像正常 DSML，但工具没有执行。
- `Read` 有时能执行，后续 `Write` 却失败，容易误判成工具白名单或参数缺失问题。
- `admin/dev/captures` 和启动日志中缺少 OpenAI 链路的工具调用诊断，排查成本较高。

本轮排查确认：问题不只是一类。既有解析器对畸形 DSML 的容错不足，也有 OpenAI 链路自身的拦截策略过严与诊断信息不足。

## 核心问题

问题最终收敛为四类：

1. **OpenAI 链路默认按 declared tools 过滤，误伤了实际工具调用**

   用户虽然走的是 OpenAI 兼容接口，但上游模型不一定严格只输出请求里声明过的工具名。

   旧逻辑中，如果当前请求只声明了 `ToolSearch`，模型后来输出 `Write`，即使 DSML 本身完整，也会被拦下。

   这会导致：

   - 模型明明已经生成完整 `Write` 调用；
   - 解析器也能读出调用结构；
   - 但最终没有转换成 OpenAI `tool_calls`。

2. **`Write.content` 中的大段 Markdown / fenced code block 被误判为“示例上下文”**

   旧逻辑为了避免把提示词里的示例工具调用误当成真实调用，会在看到某些示例特征时直接跳过。

   但 DeepSeek 在 `Write.content` 里经常会写入：

   - Markdown 标题
   - 三引号代码块
   - `thinking` / `json` / `go` 等示例片段

   结果是：工具调用本体明明完整，但因为参数内容中包含代码块，被整段误判为示例，最终不执行。

3. **DSML 对畸形 CDATA 和 wrapper 的容错不够**

   本轮实测遇到的坏格式至少包括：

   - `</|DSML|` 这种半截 closing tag
   - `|DSML|tool_calls>` 这种缺少前导 `<` 的 wrapper
   - `<![CDATA[` 开了但没有 `]]>`，直接进入 `</|DSML|parameter>`

   其中 `Bash` 失败的关键原因就是第三类：显示侧看起来“差不多对”，但实际缺了 `]]>`，旧解析器会把它视为不完整片段直接放弃。

4. **OpenAI 链路缺少可落盘的入站 / tool use 诊断**

   之前 Claude 分支已有较完整的 capture，而 OpenAI 分支只有普通访问日志：

   - 能看到 `/v1/chat/completions` 返回 200；
   - 但看不到请求里的 `messages/tools/tool_choice`；
   - 也看不到 ds2api 最终是否真的生成了 `tool_calls`。

   这会导致排查时无法快速判断问题究竟发生在：

   - 模型没生成；
   - 模型生成了但 DSML 畸形；
   - ds2api 解析失败；
   - ds2api 已解析但执行策略拦掉。

## 核心修改

1. **OpenAI 最终解析改为“宽松解析 + 仅对 `tool_choice=none` 保持阻断”**

   修改：

   - `internal/util/toolcalls_parse.go`
   - `internal/format/openai/render_chat.go`
   - `internal/format/openai/render_responses.go`
   - `internal/adapter/openai/standard_request.go`
   - `internal/adapter/openai/handler_toolcall_format.go`

   关键调整：

   - 新增 `ToolChoiceNoneBlockParseNames()` 和 `PermissiveToolParseNames()`。
   - 当请求不是 `tool_choice=none` 时，OpenAI 链路最终解析不再被 declared tools 严格卡死。
   - 只有明确 `tool_choice=none` 时，仍然保留阻断语义，避免放开过度。

   这样可以兼容“请求里声明 A，但模型输出 B”的真实场景，尤其适合 DeepSeek 这类上游并不总严格遵循工具列表的模型。

2. **避免把真实 `Write` 调用误判成示例**

   修改 `internal/util/toolcalls_parse.go`：

   - 旧逻辑：只要命中示例上下文特征，就可能直接返回空结果。
   - 新逻辑：若文本中已经存在独立可解析的 DSML/XML 工具标记，则不再因为“像示例”而整体放弃。

   同时新增 `containsStandaloneToolMarkup(...)` 作为兜底判断。

   结果是：即使 `content` 里包含复杂 Markdown、代码块、示例片段，只要外层 `Write` 调用本体完整，仍然会被执行。

3. **补上对畸形 DSML 的局部恢复能力**

   修改：

   - `internal/util/toolcalls_scan.go`
   - `internal/util/toolcalls_markup.go`
   - `internal/util/toolcalls_parse_markup.go`

   具体包括：

   - 支持识别缺少前导 `<` 的 `|DSML|tool_calls>` / `/|DSML|invoke`。
   - 当 `<![CDATA[` 缺少 `]]>` 时，如果后面已经出现 `</|DSML|parameter>` / `</parameter>`，则按“隐式 CDATA 结束”恢复。
   - 对末尾残留的孤立 `]]` 做清理，避免参数值被污染。

   这类恢复是局部的，不是无限放松：

   - 只在明确能定位到参数结束位置时恢复；
   - 不会把完全乱掉的文本强行执行成工具调用。

4. **强化 OpenAI 工具调用提示词**

   修改 `internal/adapter/openai/handler_toolcall_format.go`：

   新增三条关键约束：

   - 必须完整输出 `</|DSML|invoke>` 和 `</|DSML|tool_calls>`
   - 禁止输出半截 closing tag，如 `</|DSML|`
   - 所有 required 参数必须填入非空值

   这部分不是解析器修复，但可以显著减少上游继续生成坏 DSML 的概率。

5. **增加 OpenAI 入站与工具调用落盘诊断**

   修改：

   - `internal/adapter/openai/devcapture.go`
   - `internal/adapter/openai/handler_chat.go`
   - `internal/adapter/openai/chat_stream_runtime.go`
   - `internal/adapter/openai/responses_handler.go`
   - `internal/adapter/openai/responses_stream_runtime_core.go`
   - `internal/config/config.go`
   - `internal/config/codec.go`
   - `internal/config/store_accessors.go`
   - `internal/server/router.go`
   - `config.example.json`

   新增记录：

   - `openai_inbound`：记录 OpenAI 入站请求中的 `model/messages/input/tools/tool_choice/max_tokens`
   - `openai_tool_use`：记录最终被 ds2api 拦截并返回的工具名、参数、endpoint、finish_reason 等

   并支持通过 `config.json` 配置：

   - `dev_capture.persist_to_disk`
   - `dev_capture.output_dir`

   这样可以直接从 `logs/dev_captures` 回看：

   - 模型原始输出到底是什么；
   - 哪一次被解析成工具；
   - 哪一次因为 DSML 残缺没有被执行。

## 典型案例复盘

本轮有两个很有代表性的案例：

1. **`Write` 调用看似完整，但因为 `content` 中有大量 Markdown / code fence，被误判成示例**

   表现：

   - 前端能看到完整 `<|DSML|invoke name="Write">`
   - 也能看到 `content` 很长
   - 但最终没有执行 `Write`

   根因：

   - 旧逻辑把复杂内容误识别为提示词示例，而不是真实工具调用

   修复后：

   - 只要外层 `invoke` 完整，`content` 再复杂也能正常拦截

2. **`Bash` 看起来像正常 DSML，但实际 `CDATA` 少了 `]]>`**

   表现：

   - 显示侧能看到：
     - `<|DSML|invoke name="Bash">`
     - `parameter name="command"`
   - 但工具没有执行

   根因：

   - `command` 参数使用了未闭合 `<![CDATA[`，旧扫描器认为整段不完整

   修复后：

   - 如果后续能定位到 `</|DSML|parameter>`，则按隐式结束恢复参数值
   - 因而 `Bash` 可正常转换成最终 `tool_calls`

## 回归测试

本轮新增/增强的测试覆盖包括：

- 请求只声明 `ToolSearch`，模型输出未声明的 `Write` 时，OpenAI 链路仍能拦截执行
- `Write.content` 中包含 fenced Markdown / code block 时，仍能正常执行
- `</|DSML|` 这类半截 closing tag 不再导致完整 `invoke` 整体丢失
- `|DSML|tool_calls>` 缺少前导 `<` 时，仍可识别
- `Bash` 参数的 `<![CDATA[` 缺少 `]]>` 时，若后续参数闭合明确，仍可恢复执行
- `tool_choice=none` 时，仍然正确阻断工具调用
- `openai_tool_use` capture 会记录最终成功拦截的工具调用

验证命令：

```powershell
go test ./internal/util ./internal/adapter/openai ./internal/devcapture ./internal/config -count=1
```

## 经验总结

这次修复最值得保留的经验有三点：

1. **不要把“模型输出不标准”简单归因成上游问题**

   如果真实线上模型长期会产生某类畸形输出，而这些畸形又具备稳定模式，兼容性修复就应下沉到解析层。

2. **修复容错时要做“局部恢复”，不要做“全局放飞”**

   本轮没有简单取消 malformed guard，而是只对：

   - 半截 wrapper
   - 缺前导 `<`
   - 缺 `]]>` 但参数闭合明确

   这几类可控模式做最小恢复，风险可控。

3. **排查工具链问题时，必须同时记录“入站请求”和“最终拦截结果”**

   只看上游模型 completion 远远不够。只有把：

   - OpenAI 入站请求
   - DeepSeek 原始输出
   - ds2api 最终 tool use

   三段证据串起来，才能快速判断是“没生成”“没解析”还是“被策略挡掉”。

## 相关提交

- `82393fa fix: harden openai dsml tool-call recovery`
